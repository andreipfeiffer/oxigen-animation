export type Init = {
  element: HTMLElement;
  total_necesar: number;
};

export type Progres = {
  total_strans: number;
  donatori: number;
};

export type Donator = {
  nume: string;
  suma: number;
};
